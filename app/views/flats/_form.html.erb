<%= simple_form_for(@flat) do |f| %>
  <%= f.input :name %>
  <%= f.input :address %>
  <%= f.input :description %>
  <%= f.input :price_per_night, label: "Price per night, €", input_html: { min: 0 } %>
  <%= f.input :number_of_guests, input_html: { min: 1 } %>
  <%= f.button :submit, class: "btn btn-primary w-100" %>
<% end %>